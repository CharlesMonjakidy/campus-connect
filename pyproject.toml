[project]
name = "campus-connect"
version = "1.0.0"
description = "Campus Connect - university student platform"
requires-python = ">=3.12,<3.14"

[tool.vercel.scripts]
build = "python manage.py collectstatic --noinput"
